# Provider Verification Checklist

A Native Adapter may be enabled only after recording the provider version range, source format samples, sanitized fixtures, event mappings, unknown-field behavior, malformed-input behavior, duplicate-import behavior, version mismatch behavior, and end-to-end export tests. Until every item is complete, capability remains `unsupported` and Generic PTY remains available.
